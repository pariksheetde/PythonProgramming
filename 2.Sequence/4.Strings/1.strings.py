# STRINGS ARE HOMOGENEOUS, IMMUTABLE
EOL = "----"
lang = 'Python'
code = tuple(lang)
print(f'Conversion from strings to tuple : {code}')

print(EOL * 38)

EOL = "----"
alpha = 'abcdefghi'.upper()
lst = list(alpha)
print(f'Alphabet in List : {lst}')

print(EOL * 38)

